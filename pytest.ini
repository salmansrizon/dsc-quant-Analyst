[pytest]
testpaths = backend/tests
markers =
    integration: hits real BigQuery. Needs credentials, and is slow — the admin
        exports stream whole tables. Run the rest with: pytest -m "not integration"
