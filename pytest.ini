[pytest]
markers =
    integration: hits real BigQuery — needs credentials, and is slow (the admin
        exports stream whole tables). Skip with: pytest -m "not integration"

# Most of the suite is offline: it stubs db._client and never touches BigQuery.
# The integration tests are the ones that would have caught this session's real
# defects — a missing table, exports naming tables that never existed, a
# credential path that only broke from cwd=backend, and every mutation 403ing.
